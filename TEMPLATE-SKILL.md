---
name: <skill-name>
description: <一句话说清这个 skill 是干什么的，50 字以内。用英文逗号分隔关键词，便于 LLM 检索>
version: 1.0.0
tags: [<标签1>, <标签2>, <标签3>]
references:
  - <相对路径引用其他 skill，或省略此字段>
---

# <Skill 标题>

> **单职责**：本 skill 只做一件事 — **{用一句话精确描述}**。

## 触发条件

用户说以下任意一句时加载本 skill：
- "{触发句 1}"
- "{触发句 2}"
- "{触发句 3}"

**不要在以下场景触发**（避免越界）：
- ❌ {不属于本 skill 职责的场景}
- ❌ {容易被混淆的场景}

## 输入

- **必填**：{必须有什么才能跑}
- **可选**：{可以传什么增强输出}
- **数据源**（如适用）：{从哪里拿数据}

## 工作流

### Step 1：{步骤名}
{具体做什么，1-3 句话}

### Step 2：{步骤名}
{具体做什么}

### Step 3：{步骤名}
{具体做什么}

## 输出格式

```
{完整的输出模板，用户拿到的是这个}
```

**输出位置**：{写到哪个目录}

## 参考资源

- `{相对路径}` — {一句话说明}
- `{项目路径}` — {一句话说明}

## 红线清单（必查）

- [ ] {不可违反的硬约束 1}
- [ ] {不可违反的硬约束 2}
- [ ] {不可违反的硬约束 3}
- [ ] {项目特有约束}
- [ ] 输出必须 {具体要求}
